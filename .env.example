# Server port (default: 3000)
PORT=3000

# Allowed CORS origins (default: http://localhost:3000)
# Single origin:   CORS_ORIGIN=https://yourdomain.com
# Multiple:        CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
# Allow all:       CORS_ORIGIN=*  (not recommended for production)
CORS_ORIGIN=http://localhost:3000

# Enable verbose debug logging (default: off)
# DEBUG=1
